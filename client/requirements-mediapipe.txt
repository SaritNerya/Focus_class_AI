# חשוב: יש להתקין את הקובץ הזה עם --no-deps, כלומר:
#   pip install --no-deps -r requirements-mediapipe.txt
#
# הסיבה: מדיהפייפ מכריז (ב-metadata שלו, בכל הגרסאות) על תלות חובה ב-jax,
# jaxlib, torch, sounddevice ו-opencv-contrib-python - גם למי שלא משתמש
# בפיצ'רים האלה בכלל (אנחנו משתמשים רק ב-FaceMesh). ל-jaxlib בפרט אין
# wheel זמין עבור Python 3.8 ב-PyPI כלל, מה שהופך התקנה רגילה (pip install
# mediapipe) לבלתי אפשרית על גרסת Python הזו - ולכן מדלגים על התלויות
# המוצהרות ומתקינים ידנית רק את מה שבאמת נדרש (כבר ב-requirements.txt).
#
# חשוב שנייה: מדיהפייפ מעולם לא פרסמה גרסת 32-ביט ל-Windows בכלל (רק
# win_amd64). אם הפקודה למטה נכשלת עם "No matching distribution found",
# כמעט בוודאות שה-Python שלכם הוא 32-ביט - יש להתקין Python 64-ביט
# (ראו README).
#
# גרסת מדיהפייפ תלויה בגרסת Python: 0.10.13 ואילך הפסיקו תמיכה ב-3.8.
# ההגבלה העליונה (<0.10.22) חשובה מאוד: אימתנו בפועל שהחל מ-0.10.30
# מדיהפייפ מסירה לגמרי את ה-API הישן (mp.solutions) שעליו כל קוד הזיהוי
# כאן מבוסס (הוחלף ב-API חדש ושונה לגמרי - Tasks API). 0.10.21 היא
# הגרסה האחרונה שנבדקה ועובדת עם ה-API הישן.
mediapipe<0.10.13        ; python_version < "3.9"
mediapipe>=0.10.13,<0.10.22 ; python_version >= "3.9"
